console.log(array);

/* -------------------------------------------------------------------------- */
function arrayRange(start, end) {
    return Array(end - start + 1)
        .fill()
        .map((_, idx) => start + idx);
}

function evaluateShuffle(algorithm) {
    var array = arrayRange(0, 100);
    for (var i = 0; i < 100; i++) {}
}

function moveShuffle(array, distance) {
    //
}
